language: cpp

cache:
  apt: true

addons:
  apt:
    sources:
    - boost-latest
    packages:
    - libboost1.55-all-dev
    - libevent-dev
    - automake
    - autoconf
    - libtool
    - flex
    - bison
    - pkg-config
    - g++ 
    - libssl-dev


before_install:
  - installthrift.sh
  
cache:
  directories:
  - $HOME/thrift

script:
  - ls -R $HOME/thrift
  - cd ril && make
